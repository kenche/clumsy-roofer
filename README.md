# clumsy-roofer

### Running
Runs an HTTP server on port 8080, with logging enabled:
```
go run .
```
To disable logging, `go run . -logging=false`

### Tests
Run tests:
```
go test -v -race ./...
```
